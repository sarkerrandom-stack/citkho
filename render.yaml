services:
  - type: web
    name: khotian-scraper
    runtime: docker
    plan: starter   # <-- change to 'free' only if you accept the 512 MB risk
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"